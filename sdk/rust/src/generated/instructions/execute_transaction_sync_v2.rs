//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::SyncPayload;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const EXECUTE_TRANSACTION_SYNC_V2_DISCRIMINATOR: [u8; 8] = [90, 81, 187, 81, 39, 70, 128, 78];

/// Accounts.
#[derive(Debug)]
pub struct ExecuteTransactionSyncV2 {
    pub consensus_account: solana_address::Address,

    pub program: solana_address::Address,
}

impl ExecuteTransactionSyncV2 {
    pub fn instruction(
        &self,
        args: ExecuteTransactionSyncV2InstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ExecuteTransactionSyncV2InstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.consensus_account,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = ExecuteTransactionSyncV2InstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ExecuteTransactionSyncV2InstructionData {
    discriminator: [u8; 8],
}

impl ExecuteTransactionSyncV2InstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [90, 81, 187, 81, 39, 70, 128, 78],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ExecuteTransactionSyncV2InstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ExecuteTransactionSyncV2InstructionArgs {
    pub account_index: u8,
    pub num_signers: u8,
    pub payload: SyncPayload,
}

impl ExecuteTransactionSyncV2InstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `ExecuteTransactionSyncV2`.
///
/// ### Accounts:
///
///   0. `[writable]` consensus_account
///   1. `[optional]` program (default to `SMRTzfY6DfH5ik3TKiyLFfXexV8uSG3d2UksSCYdunG`)
#[derive(Clone, Debug, Default)]
pub struct ExecuteTransactionSyncV2Builder {
    consensus_account: Option<solana_address::Address>,
    program: Option<solana_address::Address>,
    account_index: Option<u8>,
    num_signers: Option<u8>,
    payload: Option<SyncPayload>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ExecuteTransactionSyncV2Builder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn consensus_account(&mut self, consensus_account: solana_address::Address) -> &mut Self {
        self.consensus_account = Some(consensus_account);
        self
    }
    /// `[optional account, default to 'SMRTzfY6DfH5ik3TKiyLFfXexV8uSG3d2UksSCYdunG']`
    #[inline(always)]
    pub fn program(&mut self, program: solana_address::Address) -> &mut Self {
        self.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn account_index(&mut self, account_index: u8) -> &mut Self {
        self.account_index = Some(account_index);
        self
    }
    #[inline(always)]
    pub fn num_signers(&mut self, num_signers: u8) -> &mut Self {
        self.num_signers = Some(num_signers);
        self
    }
    #[inline(always)]
    pub fn payload(&mut self, payload: SyncPayload) -> &mut Self {
        self.payload = Some(payload);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = ExecuteTransactionSyncV2 {
            consensus_account: self
                .consensus_account
                .expect("consensus_account is not set"),
            program: self.program.unwrap_or(solana_address::address!(
                "SMRTzfY6DfH5ik3TKiyLFfXexV8uSG3d2UksSCYdunG"
            )),
        };
        let args = ExecuteTransactionSyncV2InstructionArgs {
            account_index: self
                .account_index
                .clone()
                .expect("account_index is not set"),
            num_signers: self.num_signers.clone().expect("num_signers is not set"),
            payload: self.payload.clone().expect("payload is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `execute_transaction_sync_v2` CPI accounts.
pub struct ExecuteTransactionSyncV2CpiAccounts<'a, 'b> {
    pub consensus_account: &'b solana_account_info::AccountInfo<'a>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
}

/// `execute_transaction_sync_v2` CPI instruction.
pub struct ExecuteTransactionSyncV2Cpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub consensus_account: &'b solana_account_info::AccountInfo<'a>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ExecuteTransactionSyncV2InstructionArgs,
}

impl<'a, 'b> ExecuteTransactionSyncV2Cpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ExecuteTransactionSyncV2CpiAccounts<'a, 'b>,
        args: ExecuteTransactionSyncV2InstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            consensus_account: accounts.consensus_account,
            program: accounts.program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.consensus_account.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = ExecuteTransactionSyncV2InstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.consensus_account.clone());
        account_infos.push(self.program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `ExecuteTransactionSyncV2` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` consensus_account
///   1. `[]` program
#[derive(Clone, Debug)]
pub struct ExecuteTransactionSyncV2CpiBuilder<'a, 'b> {
    instruction: Box<ExecuteTransactionSyncV2CpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ExecuteTransactionSyncV2CpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ExecuteTransactionSyncV2CpiBuilderInstruction {
            __program: program,
            consensus_account: None,
            program: None,
            account_index: None,
            num_signers: None,
            payload: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn consensus_account(
        &mut self,
        consensus_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.consensus_account = Some(consensus_account);
        self
    }
    #[inline(always)]
    pub fn program(&mut self, program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn account_index(&mut self, account_index: u8) -> &mut Self {
        self.instruction.account_index = Some(account_index);
        self
    }
    #[inline(always)]
    pub fn num_signers(&mut self, num_signers: u8) -> &mut Self {
        self.instruction.num_signers = Some(num_signers);
        self
    }
    #[inline(always)]
    pub fn payload(&mut self, payload: SyncPayload) -> &mut Self {
        self.instruction.payload = Some(payload);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = ExecuteTransactionSyncV2InstructionArgs {
            account_index: self
                .instruction
                .account_index
                .clone()
                .expect("account_index is not set"),
            num_signers: self
                .instruction
                .num_signers
                .clone()
                .expect("num_signers is not set"),
            payload: self
                .instruction
                .payload
                .clone()
                .expect("payload is not set"),
        };
        let instruction = ExecuteTransactionSyncV2Cpi {
            __program: self.instruction.__program,

            consensus_account: self
                .instruction
                .consensus_account
                .expect("consensus_account is not set"),

            program: self.instruction.program.expect("program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ExecuteTransactionSyncV2CpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    consensus_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    program: Option<&'b solana_account_info::AccountInfo<'a>>,
    account_index: Option<u8>,
    num_signers: Option<u8>,
    payload: Option<SyncPayload>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
