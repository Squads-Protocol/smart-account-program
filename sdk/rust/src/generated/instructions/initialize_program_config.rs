//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const INITIALIZE_PROGRAM_CONFIG_DISCRIMINATOR: [u8; 8] = [6, 131, 61, 237, 40, 110, 83, 124];

/// Accounts.
#[derive(Debug)]
pub struct InitializeProgramConfig {
    pub program_config: solana_address::Address,
    /// The hard-coded account that is used to initialize the program config once.
    pub initializer: solana_address::Address,

    pub system_program: solana_address::Address,
}

impl InitializeProgramConfig {
    pub fn instruction(
        &self,
        args: InitializeProgramConfigInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: InitializeProgramConfigInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.program_config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.initializer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = InitializeProgramConfigInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitializeProgramConfigInstructionData {
    discriminator: [u8; 8],
}

impl InitializeProgramConfigInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [6, 131, 61, 237, 40, 110, 83, 124],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for InitializeProgramConfigInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitializeProgramConfigInstructionArgs {
    pub authority: Address,
    pub smart_account_creation_fee: u64,
    pub treasury: Address,
}

impl InitializeProgramConfigInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `InitializeProgramConfig`.
///
/// ### Accounts:
///
///   0. `[writable]` program_config
///   1. `[writable, signer, optional]` initializer (default to `init9xckLHfofCRp5SCisRK4f6eDehGRtFSAw5mLhE8`)
///   2. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct InitializeProgramConfigBuilder {
    program_config: Option<solana_address::Address>,
    initializer: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    authority: Option<Address>,
    smart_account_creation_fee: Option<u64>,
    treasury: Option<Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl InitializeProgramConfigBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn program_config(&mut self, program_config: solana_address::Address) -> &mut Self {
        self.program_config = Some(program_config);
        self
    }
    /// `[optional account, default to 'init9xckLHfofCRp5SCisRK4f6eDehGRtFSAw5mLhE8']`
    /// The hard-coded account that is used to initialize the program config once.
    #[inline(always)]
    pub fn initializer(&mut self, initializer: solana_address::Address) -> &mut Self {
        self.initializer = Some(initializer);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn smart_account_creation_fee(&mut self, smart_account_creation_fee: u64) -> &mut Self {
        self.smart_account_creation_fee = Some(smart_account_creation_fee);
        self
    }
    #[inline(always)]
    pub fn treasury(&mut self, treasury: Address) -> &mut Self {
        self.treasury = Some(treasury);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = InitializeProgramConfig {
            program_config: self.program_config.expect("program_config is not set"),
            initializer: self.initializer.unwrap_or(solana_address::address!(
                "init9xckLHfofCRp5SCisRK4f6eDehGRtFSAw5mLhE8"
            )),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = InitializeProgramConfigInstructionArgs {
            authority: self.authority.clone().expect("authority is not set"),
            smart_account_creation_fee: self
                .smart_account_creation_fee
                .clone()
                .expect("smart_account_creation_fee is not set"),
            treasury: self.treasury.clone().expect("treasury is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `initialize_program_config` CPI accounts.
pub struct InitializeProgramConfigCpiAccounts<'a, 'b> {
    pub program_config: &'b solana_account_info::AccountInfo<'a>,
    /// The hard-coded account that is used to initialize the program config once.
    pub initializer: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `initialize_program_config` CPI instruction.
pub struct InitializeProgramConfigCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub program_config: &'b solana_account_info::AccountInfo<'a>,
    /// The hard-coded account that is used to initialize the program config once.
    pub initializer: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: InitializeProgramConfigInstructionArgs,
}

impl<'a, 'b> InitializeProgramConfigCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: InitializeProgramConfigCpiAccounts<'a, 'b>,
        args: InitializeProgramConfigInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            program_config: accounts.program_config,
            initializer: accounts.initializer,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.program_config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.initializer.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = InitializeProgramConfigInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.program_config.clone());
        account_infos.push(self.initializer.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `InitializeProgramConfig` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` program_config
///   1. `[writable, signer]` initializer
///   2. `[]` system_program
#[derive(Clone, Debug)]
pub struct InitializeProgramConfigCpiBuilder<'a, 'b> {
    instruction: Box<InitializeProgramConfigCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> InitializeProgramConfigCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(InitializeProgramConfigCpiBuilderInstruction {
            __program: program,
            program_config: None,
            initializer: None,
            system_program: None,
            authority: None,
            smart_account_creation_fee: None,
            treasury: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn program_config(
        &mut self,
        program_config: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.program_config = Some(program_config);
        self
    }
    /// The hard-coded account that is used to initialize the program config once.
    #[inline(always)]
    pub fn initializer(
        &mut self,
        initializer: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.initializer = Some(initializer);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: Address) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn smart_account_creation_fee(&mut self, smart_account_creation_fee: u64) -> &mut Self {
        self.instruction.smart_account_creation_fee = Some(smart_account_creation_fee);
        self
    }
    #[inline(always)]
    pub fn treasury(&mut self, treasury: Address) -> &mut Self {
        self.instruction.treasury = Some(treasury);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = InitializeProgramConfigInstructionArgs {
            authority: self
                .instruction
                .authority
                .clone()
                .expect("authority is not set"),
            smart_account_creation_fee: self
                .instruction
                .smart_account_creation_fee
                .clone()
                .expect("smart_account_creation_fee is not set"),
            treasury: self
                .instruction
                .treasury
                .clone()
                .expect("treasury is not set"),
        };
        let instruction = InitializeProgramConfigCpi {
            __program: self.instruction.__program,

            program_config: self
                .instruction
                .program_config
                .expect("program_config is not set"),

            initializer: self
                .instruction
                .initializer
                .expect("initializer is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct InitializeProgramConfigCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    program_config: Option<&'b solana_account_info::AccountInfo<'a>>,
    initializer: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    authority: Option<Address>,
    smart_account_creation_fee: Option<u64>,
    treasury: Option<Address>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
