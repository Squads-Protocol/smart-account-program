//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CLOSE_SETTINGS_TRANSACTION_DISCRIMINATOR: [u8; 8] = [251, 112, 34, 108, 214, 13, 41, 116];

/// Accounts.
#[derive(Debug)]
pub struct CloseSettingsTransaction {
    pub settings: solana_address::Address,
    /// the logic within `settings_transaction_close` does the rest of the checks.
    pub proposal: solana_address::Address,
    /// SettingsTransaction corresponding to the `proposal`.
    pub transaction: solana_address::Address,
    /// The rent payer for the proposal account.
    pub proposal_rent_collector: solana_address::Address,
    /// The rent collector.
    pub transaction_rent_collector: solana_address::Address,

    pub system_program: solana_address::Address,
}

impl CloseSettingsTransaction {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.settings,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.proposal, false));
        accounts.push(solana_instruction::AccountMeta::new(
            self.transaction,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.proposal_rent_collector,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.transaction_rent_collector,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = CloseSettingsTransactionInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CloseSettingsTransactionInstructionData {
    discriminator: [u8; 8],
}

impl CloseSettingsTransactionInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [251, 112, 34, 108, 214, 13, 41, 116],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CloseSettingsTransactionInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `CloseSettingsTransaction`.
///
/// ### Accounts:
///
///   0. `[]` settings
///   1. `[writable]` proposal
///   2. `[writable]` transaction
///   3. `[writable]` proposal_rent_collector
///   4. `[writable]` transaction_rent_collector
///   5. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct CloseSettingsTransactionBuilder {
    settings: Option<solana_address::Address>,
    proposal: Option<solana_address::Address>,
    transaction: Option<solana_address::Address>,
    proposal_rent_collector: Option<solana_address::Address>,
    transaction_rent_collector: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CloseSettingsTransactionBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn settings(&mut self, settings: solana_address::Address) -> &mut Self {
        self.settings = Some(settings);
        self
    }
    /// the logic within `settings_transaction_close` does the rest of the checks.
    #[inline(always)]
    pub fn proposal(&mut self, proposal: solana_address::Address) -> &mut Self {
        self.proposal = Some(proposal);
        self
    }
    /// SettingsTransaction corresponding to the `proposal`.
    #[inline(always)]
    pub fn transaction(&mut self, transaction: solana_address::Address) -> &mut Self {
        self.transaction = Some(transaction);
        self
    }
    /// The rent payer for the proposal account.
    #[inline(always)]
    pub fn proposal_rent_collector(
        &mut self,
        proposal_rent_collector: solana_address::Address,
    ) -> &mut Self {
        self.proposal_rent_collector = Some(proposal_rent_collector);
        self
    }
    /// The rent collector.
    #[inline(always)]
    pub fn transaction_rent_collector(
        &mut self,
        transaction_rent_collector: solana_address::Address,
    ) -> &mut Self {
        self.transaction_rent_collector = Some(transaction_rent_collector);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CloseSettingsTransaction {
            settings: self.settings.expect("settings is not set"),
            proposal: self.proposal.expect("proposal is not set"),
            transaction: self.transaction.expect("transaction is not set"),
            proposal_rent_collector: self
                .proposal_rent_collector
                .expect("proposal_rent_collector is not set"),
            transaction_rent_collector: self
                .transaction_rent_collector
                .expect("transaction_rent_collector is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `close_settings_transaction` CPI accounts.
pub struct CloseSettingsTransactionCpiAccounts<'a, 'b> {
    pub settings: &'b solana_account_info::AccountInfo<'a>,
    /// the logic within `settings_transaction_close` does the rest of the checks.
    pub proposal: &'b solana_account_info::AccountInfo<'a>,
    /// SettingsTransaction corresponding to the `proposal`.
    pub transaction: &'b solana_account_info::AccountInfo<'a>,
    /// The rent payer for the proposal account.
    pub proposal_rent_collector: &'b solana_account_info::AccountInfo<'a>,
    /// The rent collector.
    pub transaction_rent_collector: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `close_settings_transaction` CPI instruction.
pub struct CloseSettingsTransactionCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub settings: &'b solana_account_info::AccountInfo<'a>,
    /// the logic within `settings_transaction_close` does the rest of the checks.
    pub proposal: &'b solana_account_info::AccountInfo<'a>,
    /// SettingsTransaction corresponding to the `proposal`.
    pub transaction: &'b solana_account_info::AccountInfo<'a>,
    /// The rent payer for the proposal account.
    pub proposal_rent_collector: &'b solana_account_info::AccountInfo<'a>,
    /// The rent collector.
    pub transaction_rent_collector: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> CloseSettingsTransactionCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CloseSettingsTransactionCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            settings: accounts.settings,
            proposal: accounts.proposal,
            transaction: accounts.transaction,
            proposal_rent_collector: accounts.proposal_rent_collector,
            transaction_rent_collector: accounts.transaction_rent_collector,
            system_program: accounts.system_program,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.settings.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.proposal.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.transaction.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.proposal_rent_collector.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.transaction_rent_collector.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = CloseSettingsTransactionInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(7 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.settings.clone());
        account_infos.push(self.proposal.clone());
        account_infos.push(self.transaction.clone());
        account_infos.push(self.proposal_rent_collector.clone());
        account_infos.push(self.transaction_rent_collector.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CloseSettingsTransaction` via CPI.
///
/// ### Accounts:
///
///   0. `[]` settings
///   1. `[writable]` proposal
///   2. `[writable]` transaction
///   3. `[writable]` proposal_rent_collector
///   4. `[writable]` transaction_rent_collector
///   5. `[]` system_program
#[derive(Clone, Debug)]
pub struct CloseSettingsTransactionCpiBuilder<'a, 'b> {
    instruction: Box<CloseSettingsTransactionCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CloseSettingsTransactionCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CloseSettingsTransactionCpiBuilderInstruction {
            __program: program,
            settings: None,
            proposal: None,
            transaction: None,
            proposal_rent_collector: None,
            transaction_rent_collector: None,
            system_program: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn settings(&mut self, settings: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.settings = Some(settings);
        self
    }
    /// the logic within `settings_transaction_close` does the rest of the checks.
    #[inline(always)]
    pub fn proposal(&mut self, proposal: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.proposal = Some(proposal);
        self
    }
    /// SettingsTransaction corresponding to the `proposal`.
    #[inline(always)]
    pub fn transaction(
        &mut self,
        transaction: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.transaction = Some(transaction);
        self
    }
    /// The rent payer for the proposal account.
    #[inline(always)]
    pub fn proposal_rent_collector(
        &mut self,
        proposal_rent_collector: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.proposal_rent_collector = Some(proposal_rent_collector);
        self
    }
    /// The rent collector.
    #[inline(always)]
    pub fn transaction_rent_collector(
        &mut self,
        transaction_rent_collector: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.transaction_rent_collector = Some(transaction_rent_collector);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = CloseSettingsTransactionCpi {
            __program: self.instruction.__program,

            settings: self.instruction.settings.expect("settings is not set"),

            proposal: self.instruction.proposal.expect("proposal is not set"),

            transaction: self
                .instruction
                .transaction
                .expect("transaction is not set"),

            proposal_rent_collector: self
                .instruction
                .proposal_rent_collector
                .expect("proposal_rent_collector is not set"),

            transaction_rent_collector: self
                .instruction
                .transaction_rent_collector
                .expect("transaction_rent_collector is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CloseSettingsTransactionCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    settings: Option<&'b solana_account_info::AccountInfo<'a>>,
    proposal: Option<&'b solana_account_info::AccountInfo<'a>>,
    transaction: Option<&'b solana_account_info::AccountInfo<'a>>,
    proposal_rent_collector: Option<&'b solana_account_info::AccountInfo<'a>>,
    transaction_rent_collector: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
