//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const EXTEND_TRANSACTION_BUFFER_DISCRIMINATOR: [u8; 8] = [190, 86, 246, 95, 231, 154, 229, 91];

/// Accounts.
#[derive(Debug)]
pub struct ExtendTransactionBuffer {
    pub consensus_account: solana_address::Address,

    pub transaction_buffer: solana_address::Address,
    /// The signer on the smart account that created the TransactionBuffer.
    pub creator: solana_address::Address,
}

impl ExtendTransactionBuffer {
    pub fn instruction(
        &self,
        args: ExtendTransactionBufferInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ExtendTransactionBufferInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.consensus_account,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.transaction_buffer,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.creator,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = ExtendTransactionBufferInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ExtendTransactionBufferInstructionData {
    discriminator: [u8; 8],
}

impl ExtendTransactionBufferInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [190, 86, 246, 95, 231, 154, 229, 91],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ExtendTransactionBufferInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ExtendTransactionBufferInstructionArgs {
    pub buffer: Vec<u8>,
}

impl ExtendTransactionBufferInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `ExtendTransactionBuffer`.
///
/// ### Accounts:
///
///   0. `[]` consensus_account
///   1. `[writable]` transaction_buffer
///   2. `[signer]` creator
#[derive(Clone, Debug, Default)]
pub struct ExtendTransactionBufferBuilder {
    consensus_account: Option<solana_address::Address>,
    transaction_buffer: Option<solana_address::Address>,
    creator: Option<solana_address::Address>,
    buffer: Option<Vec<u8>>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ExtendTransactionBufferBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn consensus_account(&mut self, consensus_account: solana_address::Address) -> &mut Self {
        self.consensus_account = Some(consensus_account);
        self
    }
    #[inline(always)]
    pub fn transaction_buffer(&mut self, transaction_buffer: solana_address::Address) -> &mut Self {
        self.transaction_buffer = Some(transaction_buffer);
        self
    }
    /// The signer on the smart account that created the TransactionBuffer.
    #[inline(always)]
    pub fn creator(&mut self, creator: solana_address::Address) -> &mut Self {
        self.creator = Some(creator);
        self
    }
    #[inline(always)]
    pub fn buffer(&mut self, buffer: Vec<u8>) -> &mut Self {
        self.buffer = Some(buffer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = ExtendTransactionBuffer {
            consensus_account: self
                .consensus_account
                .expect("consensus_account is not set"),
            transaction_buffer: self
                .transaction_buffer
                .expect("transaction_buffer is not set"),
            creator: self.creator.expect("creator is not set"),
        };
        let args = ExtendTransactionBufferInstructionArgs {
            buffer: self.buffer.clone().expect("buffer is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `extend_transaction_buffer` CPI accounts.
pub struct ExtendTransactionBufferCpiAccounts<'a, 'b> {
    pub consensus_account: &'b solana_account_info::AccountInfo<'a>,

    pub transaction_buffer: &'b solana_account_info::AccountInfo<'a>,
    /// The signer on the smart account that created the TransactionBuffer.
    pub creator: &'b solana_account_info::AccountInfo<'a>,
}

/// `extend_transaction_buffer` CPI instruction.
pub struct ExtendTransactionBufferCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub consensus_account: &'b solana_account_info::AccountInfo<'a>,

    pub transaction_buffer: &'b solana_account_info::AccountInfo<'a>,
    /// The signer on the smart account that created the TransactionBuffer.
    pub creator: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ExtendTransactionBufferInstructionArgs,
}

impl<'a, 'b> ExtendTransactionBufferCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ExtendTransactionBufferCpiAccounts<'a, 'b>,
        args: ExtendTransactionBufferInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            consensus_account: accounts.consensus_account,
            transaction_buffer: accounts.transaction_buffer,
            creator: accounts.creator,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.consensus_account.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.transaction_buffer.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.creator.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = ExtendTransactionBufferInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.consensus_account.clone());
        account_infos.push(self.transaction_buffer.clone());
        account_infos.push(self.creator.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `ExtendTransactionBuffer` via CPI.
///
/// ### Accounts:
///
///   0. `[]` consensus_account
///   1. `[writable]` transaction_buffer
///   2. `[signer]` creator
#[derive(Clone, Debug)]
pub struct ExtendTransactionBufferCpiBuilder<'a, 'b> {
    instruction: Box<ExtendTransactionBufferCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ExtendTransactionBufferCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ExtendTransactionBufferCpiBuilderInstruction {
            __program: program,
            consensus_account: None,
            transaction_buffer: None,
            creator: None,
            buffer: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn consensus_account(
        &mut self,
        consensus_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.consensus_account = Some(consensus_account);
        self
    }
    #[inline(always)]
    pub fn transaction_buffer(
        &mut self,
        transaction_buffer: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.transaction_buffer = Some(transaction_buffer);
        self
    }
    /// The signer on the smart account that created the TransactionBuffer.
    #[inline(always)]
    pub fn creator(&mut self, creator: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.creator = Some(creator);
        self
    }
    #[inline(always)]
    pub fn buffer(&mut self, buffer: Vec<u8>) -> &mut Self {
        self.instruction.buffer = Some(buffer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = ExtendTransactionBufferInstructionArgs {
            buffer: self.instruction.buffer.clone().expect("buffer is not set"),
        };
        let instruction = ExtendTransactionBufferCpi {
            __program: self.instruction.__program,

            consensus_account: self
                .instruction
                .consensus_account
                .expect("consensus_account is not set"),

            transaction_buffer: self
                .instruction
                .transaction_buffer
                .expect("transaction_buffer is not set"),

            creator: self.instruction.creator.expect("creator is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ExtendTransactionBufferCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    consensus_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    transaction_buffer: Option<&'b solana_account_info::AccountInfo<'a>>,
    creator: Option<&'b solana_account_info::AccountInfo<'a>>,
    buffer: Option<Vec<u8>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
