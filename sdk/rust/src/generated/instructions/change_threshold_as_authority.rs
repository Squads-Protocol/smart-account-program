//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CHANGE_THRESHOLD_AS_AUTHORITY_DISCRIMINATOR: [u8; 8] =
    [51, 141, 78, 133, 70, 47, 95, 124];

/// Accounts.
#[derive(Debug)]
pub struct ChangeThresholdAsAuthority {
    pub settings: solana_address::Address,
    /// Settings `settings_authority` that must authorize the configuration change.
    pub settings_authority: solana_address::Address,
    /// The account that will be charged or credited in case the settings account needs to reallocate space,
    /// for example when adding a new signer or a spending limit.
    /// This is usually the same as `settings_authority`, but can be a different account if needed.
    pub rent_payer: Option<solana_address::Address>,
    /// We might need it in case reallocation is needed.
    pub system_program: Option<solana_address::Address>,

    pub program: solana_address::Address,
}

impl ChangeThresholdAsAuthority {
    pub fn instruction(
        &self,
        args: ChangeThresholdAsAuthorityInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ChangeThresholdAsAuthorityInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.settings, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.settings_authority,
            true,
        ));
        if let Some(rent_payer) = self.rent_payer {
            accounts.push(solana_instruction::AccountMeta::new(rent_payer, true));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
                false,
            ));
        }
        if let Some(system_program) = self.system_program {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                system_program,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = ChangeThresholdAsAuthorityInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ChangeThresholdAsAuthorityInstructionData {
    discriminator: [u8; 8],
}

impl ChangeThresholdAsAuthorityInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [51, 141, 78, 133, 70, 47, 95, 124],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ChangeThresholdAsAuthorityInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ChangeThresholdAsAuthorityInstructionArgs {
    pub new_threshold: u16,
    pub memo: Option<String>,
}

impl ChangeThresholdAsAuthorityInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `ChangeThresholdAsAuthority`.
///
/// ### Accounts:
///
///   0. `[writable]` settings
///   1. `[signer]` settings_authority
///   2. `[writable, signer, optional]` rent_payer
///   3. `[optional]` system_program
///   4. `[]` program
#[derive(Clone, Debug, Default)]
pub struct ChangeThresholdAsAuthorityBuilder {
    settings: Option<solana_address::Address>,
    settings_authority: Option<solana_address::Address>,
    rent_payer: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    program: Option<solana_address::Address>,
    new_threshold: Option<u16>,
    memo: Option<String>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ChangeThresholdAsAuthorityBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn settings(&mut self, settings: solana_address::Address) -> &mut Self {
        self.settings = Some(settings);
        self
    }
    /// Settings `settings_authority` that must authorize the configuration change.
    #[inline(always)]
    pub fn settings_authority(&mut self, settings_authority: solana_address::Address) -> &mut Self {
        self.settings_authority = Some(settings_authority);
        self
    }
    /// `[optional account]`
    /// The account that will be charged or credited in case the settings account needs to reallocate space,
    /// for example when adding a new signer or a spending limit.
    /// This is usually the same as `settings_authority`, but can be a different account if needed.
    #[inline(always)]
    pub fn rent_payer(&mut self, rent_payer: Option<solana_address::Address>) -> &mut Self {
        self.rent_payer = rent_payer;
        self
    }
    /// `[optional account]`
    /// We might need it in case reallocation is needed.
    #[inline(always)]
    pub fn system_program(&mut self, system_program: Option<solana_address::Address>) -> &mut Self {
        self.system_program = system_program;
        self
    }
    #[inline(always)]
    pub fn program(&mut self, program: solana_address::Address) -> &mut Self {
        self.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn new_threshold(&mut self, new_threshold: u16) -> &mut Self {
        self.new_threshold = Some(new_threshold);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn memo(&mut self, memo: String) -> &mut Self {
        self.memo = Some(memo);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = ChangeThresholdAsAuthority {
            settings: self.settings.expect("settings is not set"),
            settings_authority: self
                .settings_authority
                .expect("settings_authority is not set"),
            rent_payer: self.rent_payer,
            system_program: self.system_program,
            program: self.program.expect("program is not set"),
        };
        let args = ChangeThresholdAsAuthorityInstructionArgs {
            new_threshold: self
                .new_threshold
                .clone()
                .expect("new_threshold is not set"),
            memo: self.memo.clone(),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `change_threshold_as_authority` CPI accounts.
pub struct ChangeThresholdAsAuthorityCpiAccounts<'a, 'b> {
    pub settings: &'b solana_account_info::AccountInfo<'a>,
    /// Settings `settings_authority` that must authorize the configuration change.
    pub settings_authority: &'b solana_account_info::AccountInfo<'a>,
    /// The account that will be charged or credited in case the settings account needs to reallocate space,
    /// for example when adding a new signer or a spending limit.
    /// This is usually the same as `settings_authority`, but can be a different account if needed.
    pub rent_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// We might need it in case reallocation is needed.
    pub system_program: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
}

/// `change_threshold_as_authority` CPI instruction.
pub struct ChangeThresholdAsAuthorityCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub settings: &'b solana_account_info::AccountInfo<'a>,
    /// Settings `settings_authority` that must authorize the configuration change.
    pub settings_authority: &'b solana_account_info::AccountInfo<'a>,
    /// The account that will be charged or credited in case the settings account needs to reallocate space,
    /// for example when adding a new signer or a spending limit.
    /// This is usually the same as `settings_authority`, but can be a different account if needed.
    pub rent_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// We might need it in case reallocation is needed.
    pub system_program: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ChangeThresholdAsAuthorityInstructionArgs,
}

impl<'a, 'b> ChangeThresholdAsAuthorityCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ChangeThresholdAsAuthorityCpiAccounts<'a, 'b>,
        args: ChangeThresholdAsAuthorityInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            settings: accounts.settings,
            settings_authority: accounts.settings_authority,
            rent_payer: accounts.rent_payer,
            system_program: accounts.system_program,
            program: accounts.program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.settings.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.settings_authority.key,
            true,
        ));
        if let Some(rent_payer) = self.rent_payer {
            accounts.push(solana_instruction::AccountMeta::new(*rent_payer.key, true));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
                false,
            ));
        }
        if let Some(system_program) = self.system_program {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *system_program.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = ChangeThresholdAsAuthorityInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::SQUADS_SMART_ACCOUNT_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.settings.clone());
        account_infos.push(self.settings_authority.clone());
        if let Some(rent_payer) = self.rent_payer {
            account_infos.push(rent_payer.clone());
        }
        if let Some(system_program) = self.system_program {
            account_infos.push(system_program.clone());
        }
        account_infos.push(self.program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `ChangeThresholdAsAuthority` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` settings
///   1. `[signer]` settings_authority
///   2. `[writable, signer, optional]` rent_payer
///   3. `[optional]` system_program
///   4. `[]` program
#[derive(Clone, Debug)]
pub struct ChangeThresholdAsAuthorityCpiBuilder<'a, 'b> {
    instruction: Box<ChangeThresholdAsAuthorityCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ChangeThresholdAsAuthorityCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ChangeThresholdAsAuthorityCpiBuilderInstruction {
            __program: program,
            settings: None,
            settings_authority: None,
            rent_payer: None,
            system_program: None,
            program: None,
            new_threshold: None,
            memo: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn settings(&mut self, settings: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.settings = Some(settings);
        self
    }
    /// Settings `settings_authority` that must authorize the configuration change.
    #[inline(always)]
    pub fn settings_authority(
        &mut self,
        settings_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.settings_authority = Some(settings_authority);
        self
    }
    /// `[optional account]`
    /// The account that will be charged or credited in case the settings account needs to reallocate space,
    /// for example when adding a new signer or a spending limit.
    /// This is usually the same as `settings_authority`, but can be a different account if needed.
    #[inline(always)]
    pub fn rent_payer(
        &mut self,
        rent_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.rent_payer = rent_payer;
        self
    }
    /// `[optional account]`
    /// We might need it in case reallocation is needed.
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.system_program = system_program;
        self
    }
    #[inline(always)]
    pub fn program(&mut self, program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn new_threshold(&mut self, new_threshold: u16) -> &mut Self {
        self.instruction.new_threshold = Some(new_threshold);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn memo(&mut self, memo: String) -> &mut Self {
        self.instruction.memo = Some(memo);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = ChangeThresholdAsAuthorityInstructionArgs {
            new_threshold: self
                .instruction
                .new_threshold
                .clone()
                .expect("new_threshold is not set"),
            memo: self.instruction.memo.clone(),
        };
        let instruction = ChangeThresholdAsAuthorityCpi {
            __program: self.instruction.__program,

            settings: self.instruction.settings.expect("settings is not set"),

            settings_authority: self
                .instruction
                .settings_authority
                .expect("settings_authority is not set"),

            rent_payer: self.instruction.rent_payer,

            system_program: self.instruction.system_program,

            program: self.instruction.program.expect("program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ChangeThresholdAsAuthorityCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    settings: Option<&'b solana_account_info::AccountInfo<'a>>,
    settings_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    rent_payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    program: Option<&'b solana_account_info::AccountInfo<'a>>,
    new_threshold: Option<u16>,
    memo: Option<String>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
