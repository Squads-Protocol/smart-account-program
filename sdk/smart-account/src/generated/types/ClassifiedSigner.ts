/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet'
import {
  SmartAccountSigner,
  smartAccountSignerBeet,
} from './SmartAccountSigner'
import { SessionKeyData, sessionKeyDataBeet } from './SessionKeyData'
/**
 * This type is used to derive the {@link ClassifiedSigner} type as well as the de/serializer.
 * However don't refer to it in your code but use the {@link ClassifiedSigner} type instead.
 *
 * @category userTypes
 * @category enums
 * @category generated
 * @private
 */
export type ClassifiedSignerRecord = {
  Native: { signer: SmartAccountSigner }
  SessionKey: {
    parentSigner: SmartAccountSigner
    sessionKeyData: SessionKeyData
  }
  External: { signer: SmartAccountSigner }
}

/**
 * Union type respresenting the ClassifiedSigner data enum defined in Rust.
 *
 * NOTE: that it includes a `__kind` property which allows to narrow types in
 * switch/if statements.
 * Additionally `isClassifiedSigner*` type guards are exposed below to narrow to a specific variant.
 *
 * @category userTypes
 * @category enums
 * @category generated
 */
export type ClassifiedSigner = beet.DataEnumKeyAsKind<ClassifiedSignerRecord>

export const isClassifiedSignerNative = (
  x: ClassifiedSigner
): x is ClassifiedSigner & { __kind: 'Native' } => x.__kind === 'Native'
export const isClassifiedSignerSessionKey = (
  x: ClassifiedSigner
): x is ClassifiedSigner & { __kind: 'SessionKey' } => x.__kind === 'SessionKey'
export const isClassifiedSignerExternal = (
  x: ClassifiedSigner
): x is ClassifiedSigner & { __kind: 'External' } => x.__kind === 'External'

/**
 * @category userTypes
 * @category generated
 */
export const classifiedSignerBeet = beet.dataEnum<ClassifiedSignerRecord>([
  [
    'Native',
    new beet.FixableBeetArgsStruct<ClassifiedSignerRecord['Native']>(
      [['signer', smartAccountSignerBeet]],
      'ClassifiedSignerRecord["Native"]'
    ),
  ],

  [
    'SessionKey',
    new beet.FixableBeetArgsStruct<ClassifiedSignerRecord['SessionKey']>(
      [
        ['parentSigner', smartAccountSignerBeet],
        ['sessionKeyData', sessionKeyDataBeet],
      ],
      'ClassifiedSignerRecord["SessionKey"]'
    ),
  ],

  [
    'External',
    new beet.FixableBeetArgsStruct<ClassifiedSignerRecord['External']>(
      [['signer', smartAccountSignerBeet]],
      'ClassifiedSignerRecord["External"]'
    ),
  ],
]) as beet.FixableBeet<ClassifiedSigner, ClassifiedSigner>
